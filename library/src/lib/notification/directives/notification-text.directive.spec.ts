import { NotificationTextDirective } from './notification-text.directive';

describe('NotificationTextDirective', () => {
  it('should create an instance', () => {
    const directive = new NotificationTextDirective();
    expect(directive).toBeTruthy();
  });
});
