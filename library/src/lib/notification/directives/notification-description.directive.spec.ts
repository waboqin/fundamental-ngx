import { NotificationDescriptionDirective } from './notification-description.directive';

describe('NotificationDescriptionDirective', () => {
    it('should create an instance', () => {
        const directive = new NotificationDescriptionDirective();
        expect(directive).toBeTruthy();
    });
});
