import { NotificationMetadataDirective } from './notification-metadata.directive';

describe('NotificationMetadataDirective', () => {
    it('should create an instance', () => {
        const directive = new NotificationMetadataDirective();
        expect(directive).toBeTruthy();
    });
});
