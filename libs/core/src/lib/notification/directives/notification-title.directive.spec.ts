import { NotificationTitleDirective } from './notification-title.directive';

describe('NotificationTitleDirective', () => {
    it('should create an instance', () => {
        const directive = new NotificationTitleDirective();
        expect(directive).toBeTruthy();
    });
});
